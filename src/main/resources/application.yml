spring:
  datasource:
    url: jdbc:mysql://db-petfeast..com/pet_supplies_db

    username: 
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  sql:
    init:
      mode: never

server:
  port: 8080
